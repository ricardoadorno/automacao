# HOWTO

Este arquivo agora aponta para o novo guia de usuario em `docs/howto/`.

Atalhos:
- Variaveis: use `{{chave}}` (mustache) em qualquer string.
- Env: configure `inputs.envPrefix` e `.env` no root.
- Erros: consulte `runs/<runId>/steps/<nn_stepId>/error.json`.

Leia o indice principal em:
- `docs/howto/README.md`
