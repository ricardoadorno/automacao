# API basic

## Como rodar

- `npm start -- --plan examples/api-basic/plan.json --out runs`

## O que esperar

- Um step de API que gera `evidence.html`.
- Resultado em `runs/<runId>/steps/01_get-status/`.
