# Browser basic

## Como rodar

- `npm start -- --plan examples/browser-basic/plan.json --out runs`

## O que esperar

- Um step de browser com screenshot.
- Resultado em `runs/<runId>/steps/01_open-example/screenshot.png`.
